language: node_js
node_js:
  - "6"
before_script:
  - docker run -v $TRAVIS_BUILD_DIR:/documents --name asciidoc-to-html asciidoctor/docker-asciidoctor asciidoctor -b html5 /documents/getting-started.adoc
  - git clone https://github.com/witmicko/origin-web-console.git
  - cp getting-started.html origin-web-console/app/views/
  - cd origin-web-console
  - git status
  - git config user.email "something@ff.ff"
  - git config user.name "somename"
  - git checkout -b docs_update_2 
  - git add --all
  - git commit -m "yolo"
  - git status -s  
  - git remote -v 
  - echo $GH_U
  - echo $GH_P
  - git remote add mick https://$GH_U:$GH_P@github.com/witmicko/gendocs.git
  - git remote -v 
  - git branch
  - git push mick docs_update_2
  # - git push origin docs_update  